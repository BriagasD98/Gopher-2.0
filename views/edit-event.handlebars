<article>
  <a href="/dashboard"> &larr; Back to dashboard</a>
  <h2>
    Edit Event
  </h2>

  <form class="edit-event-form">
    <div>
      <label for="event-title">Title</label>
      <input type="text" id="event-title" name="event-title" value="{{event.title}}"/>
    </div>
    <div>
        <label for="event-description">Description</label>
        <textarea name="event-description">{{event.event_description}}</textarea>      
    </div>
    <div class="field has-text-centered" id="categorySection">
            <label class="label has-text-white">Category</label>
            <div class="control">
              <div class="select">
                <select class="dropdown" name = "dropdown">
                  <option>--</option>
                  <option id="newCategory">Add a new category</option>
                </select>
              </div>
            </div>
          </div>
    
    <div class="Address">
        <div>
            <label for="event-location-street">Address</label>
            <input type="text" id="event-location-street" name="event-location-street" value="{{event.address}}" />
        </div>
    </div>
    <div>
       <label for="event-date">Date</label>
      <input type= "date" name="event-date" value="{{event.date}}"/>
    </div>
    <button type="submit" class="btn" id="submit-button">Submit</button>
  </form>

  <button class="btn" id="delete-post-btn">Delete Event</button>

</section>
</article>

<script src="/javascript/edit-event.js"></script>
<script src="/javascript/delete-event.js"></script>